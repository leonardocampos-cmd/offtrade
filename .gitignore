.venv/
__pycache__/
*.pyc
*.pyo
conferencia_precos_final.xlsx
.env
token.json
token_gmail.json
token_gmail.json.json
credentials_gmail.json
cred_drive.json
*.log
.git-rewrite/
.pipeline.lock

# Cache local de planilhas baixadas do Google Drive (baixar_planilhas_drive.py)
# e saídas mensais geradas pelo pipeline — artefatos regenerados, não código.
planilhas_cache/
nao_positivados_*.xlsx

# Inadimplencia: dado financeiro sensivel (nome de cliente + divida) —
# so vai pra VPS (deploy_static_vps.py), nunca pro GitHub Pages publico.
inadimplencia.html
inadimplencia_data.js

# Controle de Vencimento: dado local, gerado pelo uso do controle_vencimento.py
vencimento_data.json

# Auditoria de acessos: só existe de verdade na VPS (gerado a partir do log
# do nginx por exportacao_acessos.py, via cron próprio). Rodar o script
# localmente (sem esse log) so' gera um stub vazio de teste.
acessos_data.js
acessos_state.json
