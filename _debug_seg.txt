// SKUs distintos PERNOD vendidos no mes (equipe toda)
        const _todasVendasMes = typeof VENDAS_DATA !== 'undefined'
          ? vendedores.flatMap(v => (VENDAS_DATA.por_vendedor[v.nome] || {})[mes] || [])
          : [];
        const _skusPernod = new Set(
          _todasVendasMes
            .filter(s => (s.fantasia && s.fantasia.toUpperCase().includes('PERNOD'))
                      || s.produto.toUpperCase().startsWith('WHISKY JAMESON'))
            .map(s => s.produto)
        ).size;
        const _skusLabel = _skusPernod > 0
          ? `  ·  <strong>${_skusPernod}</strong> SKU${_skusPernod !== 1 ? 's' : ''} distintos vendidos`
          : '';
        rankHtml = participantes.length === 0
          ? `<div class="campanha-rank-fora">Nenhum bônus registrado ainda.</div>`
          : `<div style="margin-bottom:8px;font-size:0.72rem;color:var(--muted)">Total equipe: <strong>R
